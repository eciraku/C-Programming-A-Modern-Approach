/*
	double triangle_area(double base, height) // First error: height must have the type specified; double is only for the base variable
	double product;							  // Second error: the variable declaration must be inside the curly braces (function body)
	{
		product = base * height;
		return product / 2;
	}
*/

/*
	double triangle_area(double base, double height) 
	{
		double product;
		
		product = base * height;
		return product / 2;
	}
*/