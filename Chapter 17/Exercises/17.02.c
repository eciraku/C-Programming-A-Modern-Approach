/*
	char *duplicate(const char *str)
	{
		char *p;

		p = malloc(strlen(str) + 1);
		if (p == NULL)
		{
			printf("Error! No memory allocated!\n");
			return p;
		}

		strcpy(p, str);

		return p;
	}
*/