// Two adjacent underscores are a bad idea because it is hard to distinguish if there
// is only one underscore or more. And when you use the variable and accidentally write
// a single underscore, it will be treated as (because it is) a different variable.